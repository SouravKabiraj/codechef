public class MedianUpdates {
    public static void main(String[] args) {

    }
}

class MedianUpdatesHandler {
   private ArrayList<Integer> array = new ArrayList<Integer>();
   private boolean isOdd = false;

    MedianUpdatesHandler() {
    }

    private int midInd(){
        return array.size()/2;
    }

    public int add(int element, int index){
        if(array.isEmpty()) {
            array.add(element);
        } else {
            if(array.)
        }
    }
}